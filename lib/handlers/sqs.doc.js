/**
 * Create a handler for SQS events.
 * @function sqsHandler
 * @param {Object} options Override options passed to createHandler.
 * @returns {Object} The handler.
 * @see {@link createHandler}
 */

/**
 * Create a handler for SQS events with JSON body.
 * @function sqsJsonHandler
 * @param {Object} options Override options passed to createHandler.
 * @returns {Object} The handler.
 * @see {@link createHandler}
 */
