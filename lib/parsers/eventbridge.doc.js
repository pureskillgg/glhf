/**
 * Parser for EventBridge events.
 * Parses EventBridge rule events and redriven events from SQS.
 * @function eventbridgeParser
 * @param {Object} event The event.
 * @returns {Object[]} The parsed events.
 */
