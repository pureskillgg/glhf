/**
 * Create an invoke wrapper.
 * @function createInvokeWrapper
 * @param {Object} parentLog The parent logger.
 * @param {function} strategy The strategy.
 * @param {function} parser The parser.
 * @param {function} serializer The serializer.
 * @returns {function} The wrapper.
 */
